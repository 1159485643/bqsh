# 明星同款舆情风险复审 V3

本版按最新反馈调整：

1. 审核回写改为周期队列：
   - 连续快速点击不会立即多次请求。
   - 每 3 秒检查是否有变更，有变更才批量回写。
   - 同一行在周期内多次修改，只保留最后一次结果。
2. 后端新增批量审核回写接口，减少“请求过多”。
3. 页面顶部小字展示：
   - 后台同步状态
   - 同步周期
   - 下次同步倒计时
   - 回写队列状态
4. 领取任务进度融合到“领取任务”按钮里，不再单独占位。
5. 导入 Excel / 导出 Excel 合并到一个“Excel”按钮下拉菜单。
6. 保留原 UI、图片预览、不通过快捷原因、不通过原因不能为空校验、处理人筛选、快速同步和批量领取。

Cloudflare Pages：
- Build command 留空
- Build output directory 填 public
