# 明星同款舆情风险复审 V3

本版修复：

1. 同步飞书数据改为前端分批读取，进度条显示批次、百分比、已读取行数。
2. 领取任务改为后端批量写入，不再一行一行请求写回。
3. 历史审核状态读取修复：支持“审核状态 / 复审结果 / 是否通过 / 审核结果”等字段，已通过/已不通过等历史值可正确统计。
4. 保留原 UI、图片预览、不通过快捷原因、不通过原因不能为空校验。
5. 保留处理人筛选、全部数据读取、去内嵌数据读取、去清除记录按钮。

Cloudflare Pages：
- Build command 留空
- Build output directory 填 public
