# 明星同款舆情风险复审 V3

本版按最新反馈调整：

1. 同步每批尝试读取 4000 行，后端允许 5000 行上限；如果触发飞书 10MB 限制会自动降块。
2. 同步进度改为显示批次、已读行数、预计总行数，不再卡在 0%。
3. 增加后台静默同步：手动同步后每 3 分钟后台刷新一次；页面启动后有配置和 token 时也会尝试静默同步。
4. 领取任务不再遮挡页面，不再弹成功窗；只在“领取任务”按钮旁显示领取中/完成。
5. 领取前后端实时读取对应行，只有“处理人”为空且状态未处理的行才会批量领取。
6. 领取写回走批量接口，不再一行一行请求。
7. 保留原 UI、图片预览、不通过快捷原因、不通过原因不能为空校验、处理人筛选、历史状态读取修复。

Cloudflare Pages：
- Build command 留空
- Build output directory 填 public
